describe("spec", () => {
  beforeEach(() => {
    cy.visit("build/index.html");
  });

  context("No todos", () => {});
  context("New todo", () => {});
  context("Mark all as complete", () => {});
  context("Item", () => {});
  context("Editing", () => {});
  context("Counter", () => {});
  context("Clear completed button", () => {});
  context("Persistence", () => {});
  context("Routing", () => {});
});
